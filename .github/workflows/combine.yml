name: Append Extra M3U

on:
  workflow_run:
    workflows: ["main"]   # must match the NAME inside main.yml
    types:
      - completed
    branches:
      - main  # optional: specify which branch to monitor
  workflow_dispatch:  # keep manual trigger for testing

permissions:
  contents: write

jobs:
  append:
    if: github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # fetch all history for better commit tracking

      - name: Append playlists
        run: |
          echo "" >> main.m3u
          echo "# ===== EXTRA PLAYLISTS =====" >> main.m3u

          declare -A lists

          # YOUR PLAYLIST
          lists["AKIL_SOURCE"]="https://raw.githubusercontent.com/codedbyakil/Yogi/refs/heads/main/main.m3u"

          # ADD MORE IF NEEDED
          lists["SPORTS"]="https://example.com/sports.m3u"
          lists["MOVIES"]="https://example.com/movies.m3u"

          for name in "${!lists[@]}"
          do
            echo "" >> main.m3u
            echo "# ----- $name -----" >> main.m3u
            curl -s "${lists[$name]}" | grep -v '^#EXTM3U' >> main.m3u
          done

      - name: Commit and push
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "actions@github.com"

          git add main.m3u
          git commit -m "Append extra playlists" || echo "No changes"

          git push https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git
